"""GUIDE.pdf generator: clear space, min size, misuse examples. M4 (#26).

Not implemented yet.
"""

from __future__ import annotations

from pathlib import Path


def build_guide(client_dir: Path, out_path: Path) -> Path:
    """Generate the one-page usage guide for a sealed kit."""
    raise NotImplementedError("guide generator is implemented in M4")
